FROM ubuntu:20.04

WORKDIR /app

RUN apt-get update && apt-get install -y \
    python3 \
    python3-pip
    
RUN pip install streamlit streamlit_folium plotly pandas psycopg2-binary folium 

COPY utils/dashboard.py /app/dashboard.py

CMD [ "streamlit", "run", "dashboard.py" ]